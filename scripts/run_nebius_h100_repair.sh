#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PYTHON_BIN="${PYTHON_BIN:-python3}"

export ESM2_BACKEND="${ESM2_BACKEND:-torch}"
export ESM2_DEVICE="${ESM2_DEVICE:-cuda}"
export ESM2_DTYPE="${ESM2_DTYPE:-bf16}"
export ESM2_ENABLE_TF32="${ESM2_ENABLE_TF32:-1}"
export ESM2_BATCH_SIZE="${ESM2_BATCH_SIZE:-512}"
export ESM2_SEQUENCE_BATCH_SIZE="${ESM2_SEQUENCE_BATCH_SIZE:-64}"
export ESM2_SEQUENCE_LENGTH_BUCKET_SPAN="${ESM2_SEQUENCE_LENGTH_BUCKET_SPAN:-64}"
export ESM2_SEQUENCE_BATCH_TARGET_RESIDUES="${ESM2_SEQUENCE_BATCH_TARGET_RESIDUES:-32768}"
export TOKENIZERS_PARALLELISM="${TOKENIZERS_PARALLELISM:-false}"
export OMP_NUM_THREADS="${OMP_NUM_THREADS:-16}"
export MKL_NUM_THREADS="${MKL_NUM_THREADS:-16}"

exec "$PYTHON_BIN" "$SCRIPT_DIR/build_kimi_native_repair_dataset.py" "$@"
